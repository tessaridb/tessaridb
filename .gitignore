# Rust
/target
/target-check
/target-lint
/target-pkg
/target-ra
/target-perf
/target-sym
**/*.rs.bk
*.pdb

# Data / runtime
/data
*.db
*.rdb
*.log

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Local agent-tooling state, never published
.bgv
.bgv/
.githooks/
CLAUDE.md
AGENTS.md
MEMORY.md

# The panel's build-time toolchain. The emitted assets under
# crates/tessari-http/assets/ ARE committed — the Rust build, the Docker build
# and the release must never need Node (ADR-0076 D3).
node_modules/
